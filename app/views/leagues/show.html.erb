<%= content_for :head do %>
  <h1 class="panel panel-body">
    <%= @league.name %>
    <small><%= @league.description %></small>
  </h1>
<% end %>
<%= render partial: "leagues/show/leaderboard", locals: { report: @report } %>
<%= render partial: "leagues/show/match_predictions", locals: { report: @report } %>
